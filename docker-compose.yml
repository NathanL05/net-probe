services:
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/nginx.conf"
      - "./nginx/certs/:/etc/nginx/certs"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
